#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/.."
ROOT_DIR="$(pwd)"

EXIT_CODE=0

# Frontend
if [ -d "frontend" ]; then
  echo "== frontend: lint =="
  cd frontend
  bun run lint || EXIT_CODE=1
  cd "$ROOT_DIR"
fi

exit $EXIT_CODE

#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")/.."
ROOT_DIR="$(pwd)"

echo "== セットアップ開始 =="

# Frontend
if [ -d "frontend" ]; then
  echo "[frontend] 依存関係をインストール中..."
  cd frontend && bun install --frozen-lockfile && cd "$ROOT_DIR"
  echo "[frontend] 完了"
fi

echo "== セットアップ完了 =="
